import React from 'react';

const FilterBar = () => {
  return (
    <div className="filter-bar">
      <p className="filter-bar__placeholder">Filter and sort controls will go here.</p>
    </div>
  );
};

export default FilterBar;
